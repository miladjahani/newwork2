# راهنمای دیپلوی (نسخهٔ شخصی ادغام‌شده)

> این نسخه برای استفادهٔ شخصی و خصوصی شماست (نه بازنشر عمومی) — طبق `LICENSE`
> اصلی پروژه، لطفاً این مخزن را **Private** نگه دارید.

## این پوشه شامل چه چیزیست

```
wrangler.toml             → پیکربندی: main=_worker.js + [assets] directory=./public
package.json               → متادیتای پروژه (بدون وابستگی، بدون build)
_worker.js                 → ورکر: پروکسی CORS به api.cloudflare.com + سرو استاتیک از public/ (بدون تغییر)
public/index.html          → صفحهٔ ویزارد نصب (asset استاتیک)
public/wizard.js           → منطق ویزارد: D1 + تلگرام + ریلهٔ منطقه‌ای (asset استاتیک)
public/worker-source.js    → پنل کامل: چندکاربره + ربات تلگرام + fallback منطقه‌ای + i18n (asset استاتیک)
LICENSE                     → لایسنس اصلی پروژه (حفظ شده، دست‌نخورده)
README.md                   → README رسمی پروژه (حفظ شده، دست‌نخورده)
ips.txt                     → آی‌پی‌های کلودفلر پشت فیلترینگ ایران (کپی محلی/مرجع؛ دیپلوی نمی‌شود)
vip-list                    → فهرست JSON کشورهای دارای پروکسی VIP (کپی محلی/مرجع؛ دیپلوی نمی‌شود)
deleted-vips.txt            → آرشیو پروکسی‌های حذف‌شده (کپی محلی/مرجع؛ دیپلوی نمی‌شود)
tools/update_proxies.js     → اسکریپت جانبی Node.js برای به‌روزرسانی خودکار لیست پروکسی‌ها (خارج از Cloudflare اجرا می‌شود؛ اختیاری)
```

**نکتهٔ مهم دربارهٔ ساختار:** فایل‌هایی که باید سرو شوند (`index.html`, `wizard.js`,
`worker-source.js`) داخل `public/` هستند و مسیر `[assets] directory` در `wrangler.toml`
دقیقاً به همین پوشه اشاره می‌کند. `_worker.js` بیرون از `public/` و در ریشه می‌ماند چون
خودش فایل ورکر است، نه یک asset. `ips.txt` / `vip-list` / `deleted-vips.txt` عمداً بیرون
از `public/` نگه داشته شده‌اند چون خودِ پنل زمان اجرا این‌ها را مستقیم از
`raw.githubusercontent.com/zeus-panel/ZEUS-PANEL` می‌خواند، نه از asset محلی — پس نیازی
به دیپلوی‌شدنشان نیست؛ فقط برای مرجع/پشتیبان اینجا هستند.

**نکته دربارهٔ `ips.txt` / `vip-list`:** خودِ پنل در زمان اجرا این فایل‌ها را مستقیماً
از مخزن عمومی اصلی (`zeus-panel/ZEUS-PANEL` روی گیت‌هاب) می‌خواند — کپی محلی‌ای که
در این پوشه هست فقط برای مرجع/پشتیبان شماست و توسط ورکر استفاده نمی‌شود. اگر
می‌خواهید ورکر از مخزن خودتان بخواند، باید آدرس `raw.githubusercontent.com/zeus-panel/ZEUS-PANEL/main/`
را در `worker-source.js` (تابع‌های `fetchWithFallback` و `fetchWithFallbackUI`) به
آدرس مخزن خودتان تغییر بدهید.

## مراحل دیپلوی (GitHub → Cloudflare مستقیم)

1. محتوای این پوشه را در ریشهٔ یک مخزن **Private** گیت‌هاب پوش کنید (همین ساختار،
   بدون تغییر — `wrangler.toml` در ریشه، `public/` کنارش).
2. در Cloudflare: `Workers & Pages → Create → Connect to Git` → همان مخزن را انتخاب کنید.
3. چون `wrangler.toml` در ریشهٔ مخزن وجود دارد، Cloudflare خودش تنظیمات build را از
   آن می‌خواند (نیازی به تنظیم دستی Build command / Output directory نیست).
   دستور دیپلویی که Cloudflare اجرا می‌کند `npx wrangler deploy` است که با همین
   `wrangler.toml` (main=`_worker.js` + `[assets] directory=./public`) کاملاً سازگار است.
4. Save and Deploy — ورکر بر اساس `main` اجرا می‌شود و هر درخواستی که با پیشوند
   `/__cf/` شروع نشود را از `public/` (استاتیک) سرو می‌کند.
5. آدرس `xxxx.workers.dev` را باز کنید — ویزارد ۵ مرحله‌ای باز می‌شود.

## استفاده از ویزارد

1. یک API Token از Cloudflare بسازید (چک‌لیست دسترسی‌ها داخل ویزارد نشان داده می‌شود).
2. حساب و روش اجرا (Workers/Pages) را انتخاب کنید.
3. نام ورکر + در صورت تمایل دامنهٔ سفارشی، توکن ربات تلگرام، و لیست ریله‌های
   منطقه‌ای را وارد کنید (همه اختیاری، بعداً هم از تنظیمات پنل قابل‌تغییرند).
4. Deploy now — ویزارد دیتابیس D1 می‌سازد، `worker-source.js` را دیپلوی می‌کند،
   مسیر workers.dev را فعال می‌کند، و در صورت وارد‌کردن، تلگرام/ریله را پیکربندی می‌کند.
5. لینک پنل نمایش داده می‌شود. **همان بار اول** که پنل را باز می‌کنید رمز عبور ادمین
   را تنظیم کنید — تا قبل از تنظیم رمز، API پنل باز است.

## دربارهٔ `tools/update_proxies.js`

این اسکریپت جدا از Cloudflare است (Node.js ساده) و کارش جمع‌آوری و تست خودکار
لیست پروکسی‌های رایگان SOCKS5/HTTP از چند منبع عمومی و نوشتن نتیجه در پوشهٔ
`proxy/` است. روی Cloudflare Pages/Workers اجرا نمی‌شود و بخشی از دیپلوی پنل
نیست — فقط برای این‌که کامل داشته باشیدش این‌جا گذاشته شده. اگر خواستید خودتان
لیست پروکسی‌های VIP را به‌صورت دوره‌ای تازه کنید، می‌توانید آن را روی یک VPS یا
یک GitHub Action زمان‌بندی‌شده اجرا کنید (نیاز به `node tools/update_proxies.js`).
